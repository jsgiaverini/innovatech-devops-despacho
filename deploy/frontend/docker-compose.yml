services:
  frontend:
    image: ${ECR_REGISTRY}/front-despacho:latest
    container_name: front_despacho
    restart: unless-stopped
    environment:
      VENTAS_HOST: ${BACKEND_HOST}:8080
      DESPACHOS_HOST: ${BACKEND_HOST}:8081
    ports:
      - "80:8080"
    networks:
      - frontend_net

networks:
  frontend_net:
    driver: bridge
